[project]
name = "ibkr"
version = "0.1.0"
description = "Interactive Brokers cash inflow analysis and charts"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.0",
    "plotly>=5.18",
    "python-dotenv>=1.0",
    "requests>=2.31",
]

[tool.uv]
package = false
